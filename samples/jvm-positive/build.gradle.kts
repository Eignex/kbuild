plugins {
    id("com.eignex.jvm")
}

eignexPublish {
    description.set("Positive sample exercising the com.eignex.jvm convention (toolchain, lint, publish).")
    githubRepo.set("Eignex/kbuild")
}
